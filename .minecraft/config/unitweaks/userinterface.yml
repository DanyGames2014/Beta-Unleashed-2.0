#####################################################
##  CONFIG GENERATED BY GLASS CONFIG API (GCAPI3).  ##
##  VERIFY YOU HAVE TYPED YOUR CONFIG CORRECTLY    ##
##  BEFORE SUBMITTING BUG OR CRASH REPORTS.        ##
##  USE QUOTES (") WHEN TYPING TEXT VALUES.        ##
##  USE THE IN-GAME EDITOR WHERE POSSIBLE.         ##
#####################################################

improvedControlsMenu: true
disableDebugEntityIdTags: false
hideAchievementToast: false

# Done button will lead to Game Menu instead of unpausing
achievementBackToMenu: true

# Shows Quit Button on the Main Menu
showQuitButton: true
panoramaConfig:
  enablePanorma: false
  blurBackground: true
  # Default included : beta18, glacier
  panoramaFolder: "glacier"
videoSettingsConfig:
  # Requires a restart to take effect
  cloudHeightSlider: true
  # Requires a restart to take effect
  renderDistanceSlider: true
  # Requires a restart to take effect
  fpsLimitSlider: true
  # Requires a restart to take effect
  cloudsToggle: true
  # Requires a restart to take effect
  guiScaleSlider: true
  # Requires a restart to take effect
  brightnessSlider: true
  # Requires a restart to take effect
  fogDensitySlider: true
versionTextConfig:
  showVersionTextIngame: false
  unlicensedCopy: false
  enableCustomVersionText: false
  # Only has effect if custom version text is enabled
  customVersionText: "Minecraft Beta 1.7.3 (UniTweaks)"
